name: Playwright QA Automation CI/CD Pipeline

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]

jobs:
    test:
        timeout-minutes: 10
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Repository Code
              uses: actions/checkout@v4

            - name: Set Up Node.js Environment
              uses: actions/setup-node@v4
              with:
                  node-version: 20
                  cache: "npm"

            - name: Install Project Dependencies
              run: npm ci || npm install

            - name: Install Playwright Browsers and OS System Libraries
              run: npx playwright install --with-deps

            - name: Run E2E Test Suite (UI & API)
              # FIX: Pass --update-snapshots so the Linux runner generates its own platform baselines seamlessly
              run: npx playwright test --update-snapshots --workers=1

            - name: Archive Automated Test Execution Reports
              if: always()
              uses: actions/upload-artifact@v4
              with:
                  name: playwright-execution-report
                  path: playwright-report/
                  retention-days: 14
